fastapi==0.115.0
uvicorn[standard]==0.32.0
anthropic>=0.52.0
groq>=0.13.0
openai>=1.30.0
httpx>=0.27.0
python-multipart==0.0.12
python-dotenv==1.0.1
pydantic==2.10.0
aiofiles==24.1.0
asyncpg>=0.29.0
pypdf>=4.0.0
argon2-cffi>=23.1.0
# cryptography: Fernet for encrypting provider API keys (groq, anthropic,
# openai, whatsapp) + Salla OAuth tokens at rest. Already a transitive dep
# of many libs we use; pinning explicitly keeps version skew predictable.
cryptography>=43.0.0
# Alembic: schema migrations. psycopg2-binary is the sync driver alembic
# needs at migration time — runtime queries still go through asyncpg.
alembic>=1.13.0
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.0
# Pillow: server-side image optimisation (resize + WebP + EXIF orient/strip)
# for blog cover/inline images on upload.
Pillow>=10.2.0
